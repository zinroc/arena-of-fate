module.exports = function (knex) {
    this.knex = knex;

    return {
        /**
        * Return promise to array
        */
        getAll: function () {
            return knex("techniques").select("*").whereNot("id", 0).orderBy("id").then(function (rows) {
                return rows;
            });
        },
    };
};
