module.exports = function (knex) {
    this.knex = knex;

    return {
        /**
        * Return promise to array
        */
        getAll: function () {
            return knex("traits").select("*").orderBy("id").then(function (rows) {
                return rows;
            });
        },

    };
};
